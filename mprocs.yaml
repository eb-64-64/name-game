procs:
  frontend:
    cmd: ["bun", "run", "dev"]
    cwd: "<CONFIG_DIR>/frontend"
  backend:
    cmd: ["cargo", "run"]
    cwd: "<CONFIG_DIR>/backend"
  valkey:
    cmd: ["podman", "compose", "up", "--build", "valkey"]
    cwd: "<CONFIG_DIR>/valkey/local"
